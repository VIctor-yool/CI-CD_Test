name: CICD

on:
    push:
        branches: [master]

jobs:
  ci: # lint, test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: npm install
      - run: npm run init
      - run: npm run test